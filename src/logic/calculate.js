function Calculate(calcData, btnName) {
  let { total, next, operation } = calcData;

  if (btnName === 'AC') {
    total = '';
    next = '';
    operation = ''
    return calcData;
  }

  if (btnName === )

};

export default Calculate;